import { JwtAuthorizationMiddleware } from './jwt-authorization.middleware';
describe('JwtAuthorizationMiddleware', () => {
  it('should be defined', () => {
    // expect(new JwtAuthorizationMiddleware()).toBeDefined();
  });
});
